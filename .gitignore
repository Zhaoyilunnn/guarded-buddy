/target

# Python venv artifacts (pre-existing in this directory)
pyvenv.cfg
bin/
lib/
lib64
CACHEDIR.TAG

# Tool output
/out

# Secrets and local environment
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
**/secrets.toml

# Local IDE / agent settings (machine-specific paths)
.claude/settings.local.json
.cursor
